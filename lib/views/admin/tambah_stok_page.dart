import 'package:flutter/material.dart';

class TambahStokPage extends StatelessWidget {
  const TambahStokPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Tambah Stok Page'),
      ),
    );
  }
}