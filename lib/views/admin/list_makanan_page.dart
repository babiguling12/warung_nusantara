import 'package:flutter/material.dart';

class ListMakananPage extends StatelessWidget {
  const ListMakananPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('List Makanan Page'),
      ),
    );
  }
}